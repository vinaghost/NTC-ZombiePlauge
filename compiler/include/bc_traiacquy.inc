native bc_acquy_register(name[], delay, active, type)
native bc_acquy_name(id, name[], len)
native bc_acquy(id) 
native bc_acquy_delay(id)

forward bc_taq_kichhoat(id, id_trai)
forward bc_taq_end(id, id_trai)

enum {
	PARAMECIA,
	ZOAN,
	LOGIA
}
